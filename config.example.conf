# Copy to ${XDG_CONFIG_HOME:-~/.config}/arch-aim-trainer/config.conf if desired.
# All values can also be adjusted from the main menu.

sensitivity=0.0850
horizontal_fov=103.0
session_seconds=60
target_scale=1.00
projectile_speed=38.0
movement_speed=6.5
fullscreen=false
lead_guide=true
